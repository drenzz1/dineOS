apiVersion: 1

datasources:
  - name: Loki
    type: loki
    # Fixed uid so the provisioned dashboards (which reference uid "loki") bind
    # to this datasource explicitly instead of relying on default-datasource fallback.
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
